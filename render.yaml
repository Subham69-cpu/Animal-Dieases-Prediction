services:
  - type: web
    name: vet-healthcare-api
    runtime: docker
    dockerfilePath: Dockerfile.backend
    envVars:
      - key: MONGO_URI
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGINS
        sync: false
